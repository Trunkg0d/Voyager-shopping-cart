<div style="background: gray;  margin: auto;
  width: 50%;
  border: 3px solid green;
  padding: 10px;">
        <div style="text-align:center; color:white;padding-top:2px">
            <h2 class="display-4 fw-bolder"><%=@order_item.product.shop.name%></h2>
            <p class="lead fw-normal text-white-50 mb-0"><%=@order_item.product.shop.description%></p>
        </div>
</div>
<div style="text-align:left;  margin: auto;
  width: 50%;
  border: 3px solid green;
  padding: 10px;">
    <h2>Hello <%= @order_item.product.shop.name %> Shop,<br></h2> 
    <h4>We need you to send this order directly to the end customer without valued documents. Thank you. <br></h4>
    <h4>Send the order to:</h4>
    <h4>Customer name: <%= @order_item.order.user.name %></h4>
    <p>Address: <%= @order_item.order.user.address %></p>
    <p>Email: <%= @order_item.order.user.email %></p>
    <h4>Product: <br></h4>
    <p>Product name: <%= @order_item.product.name %></p>
    <p>Quantity: <%= @order_item.quantity %></p>
    <p>Sum money: <%= @order_item.product.price * @order_item.quantity %> $</p>
</div>






