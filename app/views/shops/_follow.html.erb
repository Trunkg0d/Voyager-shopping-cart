<div class="follow-button">
    <%= form_with(model: current_user.active_relationships.build, local: true) do |f| %>
        <div>
            <%= hidden_field_tag :followed_id, @shop.user.id %>
        </div>
        <%= f.submit "Follow", class: "btn btn-light" %>
    <% end %>
</div>